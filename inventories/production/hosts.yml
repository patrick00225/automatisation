---

all:
  hosts:
    localhost:
      ansible_connection: local

  children:
    ubuntu_servers:
      hosts:
        vm1:
          ansible_host: 192.168.1.10
        vm2:
          ansible_host: 192.168.1.11
